<?xml version="1.0" encoding="utf-8" ?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation|density"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data android:name="android.support.FILE_PROVIDER_PATHS" android:resource="@xml/file_paths" />
        </provider>

        <!-- Foreground service holding the MediaSessionCompat — see
             PlaybackService and MediaSessionPlugin. The MEDIA_BUTTON
             intent-filter lets androidx's MediaButtonReceiver (below) find
             this service to forward hardware/Bluetooth media-button events
             to when the app isn't already running it. -->
        <service
            android:name=".PlaybackService"
            android:exported="false"
            android:foregroundServiceType="mediaPlayback">
            <intent-filter>
                <action android:name="android.intent.action.MEDIA_BUTTON" />
            </intent-filter>
        </service>

        <receiver
            android:name="androidx.media.session.MediaButtonReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MEDIA_BUTTON" />
            </intent-filter>
        </receiver>
    </application>

    <!-- Permissions -->

    <uses-permission android:name="android.permission.INTERNET" />
    <!-- In-app updates: this app is sideloaded (not on Play), so it ships its
         own updater — see ApkInstallerPlugin and lib/androidUpdate.ts. Holding
         this permission only lets us *ask*; on API 26+ the user still has to
         grant "install unknown apps" for this app, which the plugin routes
         them to when it isn't granted yet. -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />
    <!-- Saving a downloaded file (playlist ZIP, lyrics export) into the public
         Downloads folder — see DownloadsPlugin. API 29+ writes there through
         MediaStore with no permission needed at all; only pre-29 needs this,
         capped with maxSdkVersion so it's a no-op grant on everything newer. -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" android:maxSdkVersion="28" />
    <!-- Media playback: a foreground Service so Android doesn't kill the app
         mid-song, plus the lock-screen/notification-shade transport control
         and audio-focus request it exists to show — see PlaybackService. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <!-- Runtime-requested (see MediaSessionPlugin) on API 33+ before the
         playback notification is shown; the service still runs foreground
         without it, just invisibly. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
</manifest>
